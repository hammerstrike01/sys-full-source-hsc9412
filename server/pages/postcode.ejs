<!DOCTYPE html>
<html lang="ko">
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width,initial-scale=1.0,minimum-scale=1.0,maximum-scale=1.0,user-scalable=no">
	<style> html, body { width: 100%; height: 100%; margin:0px; padding: 0px; background-color: #ececec; } </style>
</head>
<body>
    <div id="layer" style="width:100%; min-height: 100%;"></div>
	<script >
		window.ReactNativeWebView.postMessage("<%= name %>"+":::"+"<%= birthdate %>"+":::"+"<%= gender %>"+":::"+"<%= mobileno %>");
		// var encode = <%= EncodeData %> ;
		// console.log("success:",encode)
		// if(endcode==1){
		// 	var send_obj = {
		// 				test:'fail'
		// 				// sRtnMSG , requestnumber , responsenumber , authtype , name ,  nationalinfo , dupinfo , conninfo , mobileno , mobileco
		// 			}
		// 	window.ReactNativeWebView.postMessage(JSON.stringify(send_obj));
		// 	return
		// }
		// $.ajax({
		// 	url:'/checkplus_success',
		// 	type:"post",
		// 	data: {EncodeData:encode},
		// 	success: function(ret){ 
		// 		console.log(ret)
		// 		 var dd = ret.data; 
       
		// 		if(ret){
		// 			var send_obj = {
		// 				test:'success',
		// 				sRtnMSG:dd.sRtnMSG,
		// 				requestnumber:dd.requestnumber,
		// 				responsenumber:dd.responsenumber,
		// 				name:dd.name,
		// 				nationalinfo:dd.nationalinfo,
		// 				dupinfo:dd.dupinfo,
		// 				conninfo:dd.conninfo,
		// 				mobileno:dd.mobileno,
		// 				mobileco:dd.mobileco,
						
		// 			}
		// 			window.ReactNativeWebView.postMessage(JSON.stringify(send_obj));
		// 		}
				 
		// 	}
		// });
	</script>
</body>
</html>